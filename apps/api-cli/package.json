{
  "name": "api-cli",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/api-cli/src/server.js",
    "lint": "eslint ./src --ext .ts,.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@api-factory/core": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.0.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.6.2"
  },
  "imports": {
    "#metrics": "./metrics"
  },
  "registerMetrics": "./metrics.js"
}
