{
  "name": "user-bot",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jest": "^29.6.0"
  },
  "scripts": {
    "start": "node index.js",
  "test": "jest --config ./jest.config.cjs",
  "test:ci": "jest --config ./jest.config.cjs --runInBand",
  "test:live": "node test-api-live.js",
  "test:live:jest": "RUN_LIVE_TESTS=1 jest --config ./jest.config.cjs --runInBand",
  "test:all": "jest --config ./jest.config.cjs && node test-api-live.js"
  }
}
