{
  "name": "@api-factory/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "node ../../scripts/cleanup-dist-mocks.js",
    "test": "pnpm exec jest --config ./jest.config.cjs",
    "lint": "eslint ./src --ext .ts,.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
