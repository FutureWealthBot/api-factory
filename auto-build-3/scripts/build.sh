#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")/../.."

# Build the project
echo "Starting the build process..."

# Example build commands (customize as needed)
# Compile source code
# gcc -o output/main src/main.c

# Run any build scripts or commands
# ./scripts/some_build_script.sh

echo "Build process completed."