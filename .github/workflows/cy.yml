name: CI (pnpm monorepo)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    concurrency:
      group: api-factory-ci-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies (pnpm)
        run: pnpm install --frozen-lockfile

      - name: Lint (workspace)
        run: pnpm -w -r --if-present lint

      - name: Typecheck (workspace)
        run: pnpm -w -r --if-present typecheck

      - name: Test (workspace)
        run: pnpm -w -r --if-present test

      - name: Build (workspace)
        run: pnpm -w -r --if-present build
