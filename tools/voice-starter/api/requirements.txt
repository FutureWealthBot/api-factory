flask==2.3.3
gunicorn==20.1.0
pydub==0.25.1
python-dotenv==1.0.0
# Optional: Coqui TTS (large). Uncomment if you want high-quality local TTS.
# TTS==0.13.2
# For a lightweight fallback TTS on machines without Coqui, pyttsx3 can be used (offline, low quality)
pyttsx3==2.90
# Whisper for transcription (large install). This will also pull PyTorch wheels.
# Use a PyPI-released version. 1.1.10 is compatible with many setups.
# Use OpenAI's Whisper implementation. Installing the PyPI package `openai-whisper`
# or installing directly from the GitHub repo ensures the `whisper.load_model` API
# is available. Installing this will also pull a compatible PyTorch wheel.
openai-whisper
