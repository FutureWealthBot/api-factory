{
  "name": "fwb-workspace",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:cli": "bash -lc \"cd tools/fwb-cli && (pnpm dev || npm run dev || node index.js)\"",
    "dev:web": "bash -lc \"if [ -d apps/web ]; then cd apps/web; elif [ -d futurewealthbot-web ]; then cd futurewealthbot-web; else echo '❌ No web app folder found (apps/web or futurewealthbot-web)'; exit 1; fi; (pnpm dev || npm run dev || vite)\"",
    "dev": "concurrently -k -n CLI,WEB -c auto \"pnpm dev:cli\" \"pnpm dev:web\"",
    "build:web": "bash -lc \"cd apps/web 2>/dev/null || cd futurewealthbot-web && (pnpm build || npm run build)\"",
    "start:web": "bash -lc \"cd apps/web 2>/dev/null || cd futurewealthbot-web && (pnpm start || npm start || vite preview --port 3000)\"",
    "test": "echo \"(placeholder) add your test runner here\" && exit 0",
    "dev:cli-only": "bash -lc \"cd tools/fwb-cli && (pnpm dev || npm run dev || node server.js)\"",
    "lint": "eslint .",
    "format": "prettier -w .",
    "typecheck": "pnpm -C apps/web run typecheck || true"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@typescript-eslint/eslint-plugin": "^8.42.0",
    "@typescript-eslint/parser": "^8.42.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.35.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.42.0"
  }
}
